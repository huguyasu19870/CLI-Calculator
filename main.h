#ifndef MAIN_H_INCLUDED
#define MAIN_H_INCLUDED

int main();

/*
Design Note
---------------------------------------------------
In this CUI calculator project, I am creating calculator on CUI environment.
In the first version, input must be provided as ascii string.
Calculation that we intend to handle are;
Exponentation, Factorial, Multiplication, Division, Addition, Subtraction, and Single Comparison.
Parenthesis, Multiple Comparison, and Absolute value will be implemented in version 4.
Logarithm, trigonometry, modulo, and radical operations will be implemented in version 5.
Variable will be implemented in version 6.

In version 1, we have assume that input is correct and has no error, such as not using =< in place of <=.
In version 2, we makes sure that input is correct through internal logic.

In version 1, result output are only done in int.
In version 3, result output can be output with decimal.
*/

#endif // MAIN_H_INCLUDED
